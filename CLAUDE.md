# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new Spring Boot project (`debate-chat`) in early setup. Only IntelliJ IDEA configuration files exist so far — no source code has been written yet.

## IDE

The project is configured for IntelliJ IDEA (`.idea/` directory present). The Discord plugin is also configured (`discord.xml`).