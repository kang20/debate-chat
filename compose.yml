services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: debatechat
    ports:
      - "3311:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
